load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")
workspace(name="bug")

# Support paket dependency manager which simplifies use of nuget
#load("@rules_dotnet//dotnet:paket2bazel_dependencies.bzl", "paket2bazel_dependencies")
#paket2bazel_dependencies()

# Teamcenter required nuget packages (will need to be expanded but these support //src/solutions/csharpcust/utilities)
#load("//repo:define_nuget_repo.bzl", "define_nuget_repo")
#define_nuget_repo()
